---
import Button from "./Button.astro";
---

<div class="grid lg:grid-cols-2 gap-12 items-start">
  <!-- Contact Information -->
  <div class="space-y-8">
    <div>
      <h3 class="text-2xl font-bold text-gray-900 mb-4">
        Hablemos de tu proyecto
      </h3>
      <p class="text-lg text-gray-600 leading-relaxed">
        Nuestro equipo de expertos está listo para ayudarte a transformar tu
        visión en realidad. Obtén una consulta gratuita y descubre cómo podemos
        optimizar tu proyecto.
      </p>
    </div>

    <!-- Contact Methods -->
    <div class="space-y-6">
      <div class="flex items-start space-x-4">
        <div
          class="flex-shrink-0 w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center"
        >
          <svg
            class="w-6 h-6 text-blue-600"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
            ></path>
          </svg>
        </div>
        <div>
          <h4 class="font-semibold text-gray-900">Teléfono</h4>
          <p class="text-gray-600">+56 9 1234 5678</p>
          <p class="text-sm text-blue-600">Lun - Vie, 9:00 AM - 6:00 PM</p>
        </div>
      </div>

      <div class="flex items-start space-x-4">
        <div
          class="flex-shrink-0 w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center"
        >
          <svg
            class="w-6 h-6 text-green-600"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
            ></path>
          </svg>
        </div>
        <div>
          <h4 class="font-semibold text-gray-900">Email</h4>
          <p class="text-gray-600">contacto@watermodel3d.com</p>
          <p class="text-sm text-green-600">Respuesta en 24 horas</p>
        </div>
      </div>

      <div class="flex items-start space-x-4">
        <div
          class="flex-shrink-0 w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center"
        >
          <svg
            class="w-6 h-6 text-purple-600"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"
            ></path>
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
          </svg>
        </div>
        <div>
          <h4 class="font-semibold text-gray-900">Oficina</h4>
          <p class="text-gray-600">Santiago, Chile</p>
          <p class="text-sm text-purple-600">Visitas con cita previa</p>
        </div>
      </div>
    </div>

    <!-- Quick Stats -->
    <div class="bg-gradient-to-r from-blue-50 to-purple-50 rounded-xl p-6">
      <h4 class="font-semibold text-gray-900 mb-4">¿Por qué elegirnos?</h4>
      <div class="grid grid-cols-2 gap-4">
        <div class="text-center">
          <div class="text-2xl font-bold text-blue-600">24h</div>
          <div class="text-sm text-gray-600">Tiempo de respuesta</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-green-600">98%</div>
          <div class="text-sm text-gray-600">Satisfacción</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-purple-600">15+</div>
          <div class="text-sm text-gray-600">Años experiencia</div>
        </div>
        <div class="text-center">
          <div class="text-2xl font-bold text-orange-600">50+</div>
          <div class="text-sm text-gray-600">Proyectos</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Contact Form -->
  <div class="bg-white rounded-2xl shadow-xl p-8 border border-gray-100">
    <form class="space-y-6">
      <div class="grid md:grid-cols-2 gap-6">
        <!-- Nombre -->
        <div class="space-y-2">
          <label for="name" class="block text-sm font-medium text-gray-700"
            >Nombre *</label
          >
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-blue-500 focus:bg-white focus:ring-2 focus:ring-blue-200 transition-all duration-200 outline-none text-gray-700"
            placeholder="Tu nombre completo"
          />
        </div>

        <!-- Email -->
        <div class="space-y-2">
          <label for="email" class="block text-sm font-medium text-gray-700"
            >Email *</label
          >
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-blue-500 focus:bg-white focus:ring-2 focus:ring-blue-200 transition-all duration-200 outline-none text-gray-700"
            placeholder="tu@empresa.com"
          />
        </div>
      </div>

      <!-- Empresa y Teléfono -->
      <div class="grid md:grid-cols-2 gap-6">
        <div class="space-y-2">
          <label for="company" class="block text-sm font-medium text-gray-700"
            >Empresa</label
          >
          <input
            type="text"
            id="company"
            name="company"
            class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-blue-500 focus:bg-white focus:ring-2 focus:ring-blue-200 transition-all duration-200 outline-none text-gray-700"
            placeholder="Nombre de tu empresa"
          />
        </div>

        <div class="space-y-2">
          <label for="phone" class="block text-sm font-medium text-gray-700"
            >Teléfono</label
          >
          <input
            type="tel"
            id="phone"
            name="phone"
            class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-blue-500 focus:bg-white focus:ring-2 focus:ring-blue-200 transition-all duration-200 outline-none text-gray-700"
            placeholder="+56 9 1234 5678"
          />
        </div>
      </div>

      <!-- Tipo de Proyecto -->
      <div class="space-y-2">
        <label
          for="project-type"
          class="block text-sm font-medium text-gray-700"
          >Tipo de Proyecto *</label
        >
        <select
          id="project-type"
          name="project-type"
          required
          class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-blue-500 focus:bg-white focus:ring-2 focus:ring-blue-200 transition-all duration-200 outline-none text-gray-700"
        >
          <option value="" disabled selected
            >Selecciona el tipo de proyecto</option
          >
          <option value="planta-tratamiento"
            >Planta de Tratamiento Completa</option
          >
          <option value="modelado-bim">Modelado BIM Específico</option>
          <option value="simulacion">Simulación de Procesos</option>
          <option value="documentacion">Documentación Técnica</option>
          <option value="consultoria">Consultoría Especializada</option>
          <option value="otro">Otro</option>
        </select>
      </div>

      <!-- Presupuesto -->
      <div class="space-y-2">
        <label for="budget" class="block text-sm font-medium text-gray-700"
          >Presupuesto Estimado</label
        >
        <select
          id="budget"
          name="budget"
          class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-blue-500 focus:bg-white focus:ring-2 focus:ring-blue-200 transition-all duration-200 outline-none text-gray-700"
        >
          <option value="">Selecciona un rango</option>
          <option value="5k-15k">$5,000 - $15,000 USD</option>
          <option value="15k-50k">$15,000 - $50,000 USD</option>
          <option value="50k-100k">$50,000 - $100,000 USD</option>
          <option value="100k+">$100,000+ USD</option>
          <option value="por-definir">Por definir</option>
        </select>
      </div>

      <!-- Mensaje -->
      <div class="space-y-2">
        <label for="message" class="block text-sm font-medium text-gray-700"
          >Mensaje *</label
        >
        <textarea
          id="message"
          name="message"
          rows="5"
          required
          class="w-full px-4 py-3 rounded-lg bg-gray-50 border border-gray-200 focus:border-blue-500 focus:bg-white focus:ring-2 focus:ring-blue-200 transition-all duration-200 outline-none text-gray-700 resize-none"
          placeholder="Cuéntanos sobre tu proyecto, objetivos específicos, cronograma y cualquier detalle relevante que nos ayude a entender mejor tus necesidades..."
        ></textarea>
      </div>

      <!-- Términos y Condiciones -->
      <div class="flex items-start space-x-3">
        <input
          type="checkbox"
          id="terms"
          name="terms"
          required
          class="mt-1 h-4 w-4 text-blue-600 border-gray-300 rounded focus:ring-blue-500 focus:ring-2"
        />
        <label for="terms" class="text-sm text-gray-600 leading-relaxed">
          Acepto los <a
            href="#"
            class="text-blue-600 hover:text-blue-700 underline"
            >términos y condiciones</a
          >
          y autorizo el tratamiento de mis datos para responder a mi consulta.
          <span class="text-blue-600"
            >No spam, solo respuestas profesionales.</span
          >
        </label>
      </div>

      <!-- Submit Button -->
      <button
        type="submit"
        class="w-full bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white font-semibold py-4 px-8 rounded-lg transition-all duration-300 transform hover:scale-[1.02] shadow-lg hover:shadow-xl flex items-center justify-center space-x-2"
      >
        <span>Enviar Consulta</span>
        <svg
          class="w-5 h-5"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"></path>
        </svg>
      </button>

      <p class="text-center text-sm text-gray-500">
        Nos pondremos en contacto contigo en un plazo máximo de 24 horas
      </p>
    </form>
  </div>
</div>

<script>
  const form = document.querySelector("form");
  form?.addEventListener("submit", (e) => {
    e.preventDefault();
    const formData = new FormData(e.target as HTMLFormElement);
    const data = Object.fromEntries(formData);

    // Aquí puedes agregar tu lógica de envío
    console.log("Form data:", data);

    // Simular envío exitoso
    alert("¡Mensaje enviado con éxito!");
    (e.target as HTMLFormElement).reset();
  });
</script>
